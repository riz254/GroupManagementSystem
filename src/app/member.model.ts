export interface Member {
  id?: number;
  // groupId: number;
  firstname: string;
  lastname: string;
  nationalid: string;
  phonenumber: string;
  savings: number; // Savings can be initialized as 0 where necessary
}
